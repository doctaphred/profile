#!/usr/bin/env bash
set -euo pipefail

# Search biblegateway.

# NOTE: Use "$*", not "$@", to avoid expanding into multiple strings.
open "https://www.biblegateway.com/passage/?version=ESV;AMP&search=$*"
