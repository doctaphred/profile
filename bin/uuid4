#!/usr/bin/env python3
"""Print a random version 4 UUID.

NOTE: On Linux, just use `</proc/sys/kernel/random/uuid`.
NOTE: On FreeBSD, just use `</compat/linux/proc/sys/kernel/random/uuid`.
"""
from uuid import uuid4
print(uuid4(), end='')
